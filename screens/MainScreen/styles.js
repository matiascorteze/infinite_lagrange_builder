import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 30,
    paddingTop: 70,
  },
  headline: {
    fontSize: 30,
    color: '#003049'
  }
});

export default styles