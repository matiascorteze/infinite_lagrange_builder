import { StyleSheet } from 'react-native'

export default {
  primary: '#003049',
  secondary: '#cacaca',
}

const globalStyles = StyleSheet.create({
  centered: {
    alignItems: 'center',
  }
})

export { globalStyles }
